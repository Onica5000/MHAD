<?xml version="1.0" encoding="utf-8"?>
<!--
  Round-launcher adaptive icon. Same content as ic_launcher.xml; the
  launcher applies its own mask, so we just point at the same drawables.
  See ic_launcher.xml for the placeholder note + replacement workflow.
-->
<adaptive-icon xmlns:android="http://schemas.android.com/apk/res/android">
    <background android:drawable="@color/ic_launcher_background" />
    <foreground android:drawable="@mipmap/ic_launcher" />
</adaptive-icon>
