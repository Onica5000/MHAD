# PDF Renderer Audit & Globalization — Tracker (2026-07-09)

Branch `feat/pdf-globalization` (stacked on `feat/ai-audit-fixes`, PR #8).
Goal: blank-form-first globalized rendering (shared parameterized section
builders + per-form ordering manifests), all data printed in the right place,
verified by rasterized before/after diffs (`tool/pdf_compare.py`, PyMuPDF).

**Fidelity rule (user-approved):** pixel-identical to the pre-refactor
baseline except itemized deliberate fixes; improvements allowed where they
better match the official booklet — every changed page must map to a row here.

## Phase log

| Phase | What | Status | Commit |
|---|---|---|---|
| P0 | Render harness emits 6 artifacts (filled+blank × 3 forms); `tool/pdf_compare.py` baseline/diff (120 dpi grayscale, 0.05% page threshold); baseline captured (33 pages, self-diff 0.000% → rendering is deterministic). | ✅ | c837a15 |
| P1 | Extract duplicated statutory prose → `pdf_form_text.dart`; verified verbatim against official `PA MHAD.pdf` with page citations. **Reconciliation finding: the "drift" is official** — the booklet's own forms vary (Combined's revocation says "this Power of Attorney" mid-paragraph, p. 25; only the Declaration says "Two years after", p. 36). Official quirks preserved; deliberate app deviations documented (POA "(4)(5)(6)" typo normalized, "Principle Signature" corrected, hospitalization block + facility checkboxes + triggers + conditional rows + current-meds table are app additions). Defect #7 recharacterized: no wording changes needed. | ✅ | 062e754 |
| P2 | `pdf_form_sections.dart` parameterized builders; 3 renderers → ordering manifests (~200 lines each, from 880/601/722). **Gate passed: 0.000% pixel difference on all 33 pages, first pass.** | ✅ | 062e754 |
| P3 | Placement fixes: POA room prefs + primary doctor (defects #4/#5); Declaration guardian City/State/Zip (#11); conditional `medicationConsent` printing (#6); watermark on legal-language path (#9); wallet-card fonts via shared `pdf_theme.dart` (#10). Every diffed page visually verified against the allow-list below; blanks + Combined stayed 0.000%. | ✅ | 79e22ac |
| P4 | `attachments_pdf.dart`: Attachment A (Allergies & Adverse Reactions), B (Crisis Plan — the five `crisisPlanJson` lists), C (Self-Binding Acknowledgment), rendered only when data exists (blanks stay 0.000%). `allergies` plumbed through `DirectiveBundle`/`loadBundle` → `export_screen` (all 3 generate calls) → `PdfGenerator.generate()`. New attachments page visually verified; footer "Page N of M" count change (0.003-0.004%) on existing pages is the expected side effect. Test: attachments add pages only when data exists (allergy-only and crisis-plan-only cases). Defects #1-#3 closed. | ✅ | 206f889 |
| P5 | `showBlankFormPicker` (all three `FormType.displayName` options) wired on the web-landing tool card and a new export-screen "Print a blank form (fill in by hand)" action. Defect #8 closed. | ✅ | 206f889 |
| P6 | Final gate passed: every diffed page mapped to the allow-list (declaration guardian row, POA doctor/room reflow, +1 attachments page per filled form, footer page-count ripple); blanks 0.000% throughout. 355 tests green, analyze clean, release web build passes. Baselines re-captured at the final state (36 pages). | ✅ | (this commit) |

## Defect register (from the audit)

| # | Defect | Fixed in |
|---|---|---|
| 1 | Allergies (entire wizard step 8) never printed on any form; not even a `generate()` parameter. | P4 |
| 2 | `crisisPlanJson` never printed on any form. | P4 |
| 3 | `selfBindingEnabled` (Ulysses acknowledgment) never printed. | P4 |
| 4 | POA drops room/environment preferences (rendered on Combined + Declaration). | P3 |
| 5 | POA omits the primary-care-doctor line. | P3 |
| 6 | `medicationConsent` has no `conditional:` printing (ECT/experimental/drug-trial do) — live since the AI-audit made conditional medication consent reachable. | P3 |
| 7 | Statutory prose duplicated across renderers with wording drift (fidelity bug). | P1 |
| 8 | Blank form UI hardwired to Combined; blank Declaration/POA unreachable. | P5 |
| 9 | `legalLanguage` path skips the draft watermark. | P3 |
| 10 | Wallet card ignores the embedded theme fonts (always Helvetica). | P3 |
| 11 | Declaration's guardian-nominee block drops the City/State/Zip line (official form p. 37 has it; Combined/POA render it). Found during P1. | P3 |

## Prose reconciliations (P1) — filled in as they happen

| Paragraph | Variants found | Resolution |
|---|---|---|

## Expected visual diffs (P3/P4) — the allow-list for the final gate

| Artifact/page | Change | Defect # |
|---|---|---|
| filled_declaration p4 (0.311%) | Guardian nominee gains the City/State/Zip + Phone two-column row (was phone-only). Verified in diff image: only that row changed. | #11 |
| filled_poa p1 (5.6%) | "Primary Care Doctor" line added after Date of Birth. Verified in diff image. | #5 |
| filled_poa p2-p7 (2-20%) | Pure reflow cascade from the p1 line + the "Room and environment preferences" block on the facility page. | #4, #5 |
| blank_* (all) + filled_combined | **0.000%** — new blocks hide when empty; Combined already rendered everything. | — |
| (not in artifacts) | Legal-language pages now watermark drafts; wallet card uses the embedded editorial fonts. | #9, #10 |
| (dormant until data) | Conditional medication consent rows print when `medicationConsent` is `conditional:<text>`. | #6 |

Baselines re-captured after acceptance (P3 state is the new reference).
