# AI Prompting & Hardening Audit — Findings & Fix Tracker (2026-07-08)

Audit of the AI autofill/prompting stack against the requirement: **every user-fillable
field must be available for the AI to fill, with correct end-to-end key mapping**, plus
prompt accuracy/completeness and hardening. Branch: `feat/ai-audit-fixes` (based on
`chore/code-review-2026-07-08` — same files; retarget to main after PR #7 merges).

**What checked out clean:** extraction prompt prose ↔ schema ↔ `fromJson` are fully
consistent (all keys described, parsed, spelled right); `_formScope` POA/Declaration
lists cover all 28 data keys with no gaps; smart-fill JSON keys match `fromJson`
exactly; chat + grounded + consistency-review paths are consent-gated, PII-stripped,
and injection-defended; the clinical policy reaches every advisory path.

## Package A — apply-path bugs (AI fills it, app drops it)

| # | Finding | Fix | Status | Commit |
|---|---|---|---|---|
| A1 | 12 of 18 `SmartFillResult` fields shown with checkboxes but never persisted (`pipeline_apply_service.dart` reads only 6 display keys). | Persisted with safe targets: religious/children/family/records/pet → their exact columns; de-escalation + triggers → crisisIntervention (labeled); facility-type notes → labeled in `other`. **Deviation:** the 3 procedure-guidance fields (ECT/experimental/drug-trial) are explicitly display-only with a UI caption — meta-guidance must not print into a legal form. | ✅ | 1cfa881 |
| A2 | Review-screen edits to medication rows discarded — apply re-reads validated objects, not `_reviewEdited`. | `medFromReview` composite parser honors edits (unchanged-detection guard; current-med dosage vs preference reason formats handled separately; badges stripped). | ✅ | 1cfa881 |
| A3 | Verbatim `effective_condition` text lost (validator → ICD chips; apply writes canned sentence). | Raw text carried through `ValidatedExtractionResult` → editable "When this kicks in (your words)" review row → apply prefers it over the canned sentence. | ✅ | 1cfa881 |
| A4 | Allergy `notes` extracted + parsed then dropped (`addAllergy` omits the column). | Notes included in the insert companion. | ✅ | 1cfa881 |
| A5 | Phone conflict-detection reads `homePhone`; all writes use `cellPhone` → existing phone never flagged before overwrite. | Reads `cellPhone` for agent + alternate. | ✅ | 1cfa881 |

## Package B — hardening

| # | Finding | Fix | Status | Commit |
|---|---|---|---|---|
| B1 | No prompt-injection defense on the untrusted-document paths (extraction, consolidate, audio) — the one PII-carrying path. Chat has the rule; these don't. | SECURITY blocks added to all three prompts: document/notes/audio are data, never instructions. | ✅ | 472c335 |
| B2 | Wizard rail suggestions auto-fire before per-session AI consent (`wizardRailSuggestionsProvider` never checks `aiConsentGivenProvider`; every other path gates). | Provider returns null until consent; `watch`es so it activates the moment consent is granted. | ✅ | 472c335 |
| B3 | `document_relevant=false` scrub is model-enforced only — pipeline merges every result regardless; a mixed batch can absorb an irrelevant doc's fields. | `fromJson` returns an empty result (flag + kind only) when the flag is false; 3 tests + new-fields scrub test. | ✅ | 472c335 |
| B4 | Smart-fill PII stripping partial: med/condition list entries inserted raw (a name typed in a med field reaches the API). | All list entries pass the `PiiStripper` chokepoint; test added. | ✅ | 472c335 |

## Package C — field-coverage gaps ("every field available to the AI")

| # | Gap | Fix | Status | Commit |
|---|---|---|---|---|
| C1 | `medicationConsent` (the 4th consent) not extractable; the 3 statutory triggers land as free text while the wizard uses checkboxes; no `conditional:` consent value expressible. | Full pipeline extension: shared CONSENT VALUES vocabulary (incl. `conditional: <restriction>` → `conditional:` prefix), `medication_consent`, three explicit-only trigger booleans applied via `updateEffectiveCondition` preserving text. | ✅ | 73e1382 |
| C2 | Guardian conditions (canRevoke / canChangeAgent / mustConsultAgent + notes) not extractable. | Booleans + verbatim notes end to end; guardian upsert extended non-destructively. | ✅ | b28ee79 |
| C3 | Room-preference chips (beyond same-gender) and roommate gender detail (women/men/sameAsIdentity) not extractable. | Chip extraction clamped to the wizard's ids; chips now MERGE with existing selections (the old code replaced the csv); gender match honors the extracted value. | ✅ | b28ee79 |
| C4 | Structured crisis plan (`crisisPlanJson`) unreachable by AI, even though smart-fill already generates triggers/de-escalation. | `ExtractedCrisisPlan` (5 lists, exact screen key shape) → confirmable review row → list-union merge into `crisisPlanJson`. | ✅ | b28ee79 |

**Confirmed deliberate exclusions (no action):** witnesses (wet-ink at execution),
execution/expiration dates (signing actions), agent-acceptance log (manual by design),
side-effects JSON (own dedicated AI flow), allergy code/codeSource (NIH validation
fills), agent home/work phones (single extracted phone → cellPhone is documented).

## Package D — prompt accuracy

| # | Finding | Fix | Status | Commit |
|---|---|---|---|---|
| D1 | Smart-fill NTI instruction targets a "reason field" that doesn't exist in its output. | Reworded: monitoring requirements may be noted in `health_history`, never as a medication suggestion. | ✅ | (D commit) |
| D2 | Chat system prompt reuses rule numbers 19/20 twice (merge artifact). | Role-integrity renumbered 21–24, PII rules 25–27 — every rule number now unique. | ✅ | (D commit) |
| D3 | Two divergent clinical-policy copies (chat inline vs `aiClinicalPolicy`) despite "keep in sync" comment. | Semantics aligned to the stricter wording ("reduce" added to the shared constant's NEVER list) and both sync comments now name each other + the must-match rule. Full structural unification deliberately not done — the chat version is an intentional expansion. | ✅ | (D commit) |
| D4 | Smart-fill log says "of 13 possible" (it's 18). | Count derived from the actual map. | ✅ | (D commit) |
| D5 | **⚠ Needs YOUR legal verification:** AI prompts cite the ECT/experimental/drug-trial initialing rule as `20 Pa.C.S. §5805(c)(4)`; the app's own reference labels §5805 "Provider Immunity". One is mislabeled. **No prompt text changed until you confirm the correct cite against the statute / PA MHAD.pdf.** | Flagged only. | ⚠ awaiting user | |
