# AI Prompting & Hardening Audit — Findings & Fix Tracker (2026-07-08)

Audit of the AI autofill/prompting stack against the requirement: **every user-fillable
field must be available for the AI to fill, with correct end-to-end key mapping**, plus
prompt accuracy/completeness and hardening. Branch: `feat/ai-audit-fixes` (based on
`chore/code-review-2026-07-08` — same files; retarget to main after PR #7 merges).

**What checked out clean:** extraction prompt prose ↔ schema ↔ `fromJson` are fully
consistent (all keys described, parsed, spelled right); `_formScope` POA/Declaration
lists cover all 28 data keys with no gaps; smart-fill JSON keys match `fromJson`
exactly; chat + grounded + consistency-review paths are consent-gated, PII-stripped,
and injection-defended; the clinical policy reaches every advisory path.

## Package A — apply-path bugs (AI fills it, app drops it)

| # | Finding | Fix | Status | Commit |
|---|---|---|---|---|
| A1 | 12 of 18 `SmartFillResult` fields shown with checkboxes but never persisted (`pipeline_apply_service.dart` reads only 6 display keys). | Persist all with safe targets: religious/children/family/records/pet → their exact columns; de-escalation + triggers → crisisIntervention; facility notes → labeled in `other`. **Deviation:** the 3 procedure-guidance fields (ECT/experimental/drug-trial) become explicitly display-only with a UI caption — they are meta-guidance ("help the user state their own preference") and must not print into a legal form. | ☐ | |
| A2 | Review-screen edits to medication rows discarded — apply re-reads validated objects, not `_reviewEdited`. | Apply honors `_reviewEdited` for med rows (safe name—reason parse; badges stripped). | ☐ | |
| A3 | Verbatim `effective_condition` text lost (validator → ICD chips; apply writes canned sentence). | Carry raw text through `ValidatedExtractionResult` → editable review row → apply. | ☐ | |
| A4 | Allergy `notes` extracted + parsed then dropped (`addAllergy` omits the column). | Include notes in companion + review display. | ☐ | |
| A5 | Phone conflict-detection reads `homePhone`; all writes use `cellPhone` → existing phone never flagged before overwrite. | Read `cellPhone` at `pipeline_review_widget.dart:361,370`. | ☐ | |

## Package B — hardening

| # | Finding | Fix | Status | Commit |
|---|---|---|---|---|
| B1 | No prompt-injection defense on the untrusted-document paths (extraction, consolidate, audio) — the one PII-carrying path. Chat has the rule; these don't. | SECURITY block: document/audio content is data, never instructions. | ☐ | |
| B2 | Wizard rail suggestions auto-fire before per-session AI consent (`wizardRailSuggestionsProvider` never checks `aiConsentGivenProvider`; every other path gates). | Provider returns null until consent; watches so it activates after acceptance. | ☐ | |
| B3 | `document_relevant=false` scrub is model-enforced only — pipeline merges every result regardless; a mixed batch can absorb an irrelevant doc's fields. | `fromJson` returns an empty result (flag + kind only) when the flag is false; unit test. | ☐ | |
| B4 | Smart-fill PII stripping partial: med/condition list entries inserted raw (a name typed in a med field reaches the API). | Strip list entries too; extend `smart_fill_prompt_test`. | ☐ | |

## Package C — field-coverage gaps ("every field available to the AI")

| # | Gap | Fix | Status | Commit |
|---|---|---|---|---|
| C1 | `medicationConsent` (the 4th consent) not extractable; the 3 statutory triggers land as free text while the wizard uses checkboxes; no `conditional:` consent value expressible. | Extend schema + prompt + review + apply. | ☐ | |
| C2 | Guardian conditions (canRevoke / canChangeAgent / mustConsultAgent + notes) not extractable. | Extend schema + prompt + review + apply. | ☐ | |
| C3 | Room-preference chips (beyond same-gender) and roommate gender detail (women/men/sameAsIdentity) not extractable. | Chip-id + gender-match extraction keyed to the wizard's chip set. | ☐ | |
| C4 | Structured crisis plan (`crisisPlanJson`) unreachable by AI, even though smart-fill already generates triggers/de-escalation. | Structured crisis-plan extraction → review → crisisPlanJson. | ☐ | |

**Confirmed deliberate exclusions (no action):** witnesses (wet-ink at execution),
execution/expiration dates (signing actions), agent-acceptance log (manual by design),
side-effects JSON (own dedicated AI flow), allergy code/codeSource (NIH validation
fills), agent home/work phones (single extracted phone → cellPhone is documented).

## Package D — prompt accuracy

| # | Finding | Fix | Status | Commit |
|---|---|---|---|---|
| D1 | Smart-fill NTI instruction targets a "reason field" that doesn't exist in its output. | Reword to the fields that do exist. | ☐ | |
| D2 | Chat system prompt reuses rule numbers 19/20 twice (merge artifact). | Renumber. | ☐ | |
| D3 | Two divergent clinical-policy copies (chat inline vs `aiClinicalPolicy`) despite "keep in sync" comment. | Chat composes the shared constant + chat-specific additions. | ☐ | |
| D4 | Smart-fill log says "of 13 possible" (it's 18). | Derive count. | ☐ | |
| D5 | **⚠ Needs YOUR legal verification:** AI prompts cite the ECT/experimental/drug-trial initialing rule as `20 Pa.C.S. §5805(c)(4)`; the app's own reference labels §5805 "Provider Immunity". One is mislabeled. **No prompt text changed until you confirm the correct cite against the statute / PA MHAD.pdf.** | Flagged only. | ⚠ awaiting user | |
